var resourcesList = [
	new ResourceModel({
		id: 'numbers', name: 'Numbers',
		sign: '#', rateSign: 'Hz',
		visible: true, initialAmount: 13.5,
	}),
];

var resources = resourcesFromList(resourcesList);
